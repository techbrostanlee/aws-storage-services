# aws-storage-services
this project demonstrates how to:  Create an S3 bucket Upload, retrieve, and delete objects Configure permissions for public access Host a static website using HTML files Use AWS CLI for bucket and object management
